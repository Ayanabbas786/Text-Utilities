This website is made using HTML, Django, and Bootstrap CSS.

You need to download Django by entering "pip install django" in terminal.

After that, enter "python <path to manage.py> runserver" to start the server.